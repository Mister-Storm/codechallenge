name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Run Checkstyle (Linter)
        run: mvn checkstyle:check

      - name: Run tests with coverage
        run: mvn clean test jacoco:report

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: target/site/jacoco/
          retention-days: 3

      - name: Display coverage summary
        run: |
          if [ -f "target/site/jacoco/jacoco.csv" ]; then
            echo "📊 Cobertura de Código"
            echo "======================"
            # Extrai as métricas principais de forma mais robusta
            grep "GROUP,br" target/site/jacoco/jacoco.csv | awk -F',' '{
              printf "→ Cobertura de Instruções: %.2f%%\n", $4
              printf "→ Cobertura de Branches: %.2f%%\n", $5
              printf "→ Complexidade Ciclomática: %.2f\n", $7
            }'
            echo ""
            echo "Relatório completo disponível em target/site/jacoco/index.html"
          else
            echo "❌ Relatório não gerado. Verifique:"
            echo "1. Se há classes em src/main/java"
            echo "2. Se os testes foram executados com sucesso"
            echo "3. Estrutura de pastas:"
            ls -R target/site/ || echo "Nenhum relatório encontrado"
          fi